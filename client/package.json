{
  "scripts": {
    "build": "browserify ./index.js -s devServer.ClientModule > ./dist/client.js"
  },
  "browserify": {
    "transform": [
      ["babelify", {"presets": ["es2015", "stage-2"]}]
    ]
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-stage-2": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  }
}
